argparse
easydict
h5py
matplotlib
numpy
open3d # if you're on newer versions of python / mamba / conda, installing may throw an error. installing latest open3d seems to work 
opencv-python
pyyaml
scipy
tensorboardX
timm  # if you're on newer versions of python / mamba / conda, installing may throw an error. installing latest timm seems to work 
tqdm
transforms3d
einops
